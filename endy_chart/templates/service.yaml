apiVersion: v1
kind: Service
metadata:
  name: {{ include "endy_chart.fullname" . }}
  labels:
    {{- include "endy_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "endy_chart.selectorLabels" . | nindent 4 }}
